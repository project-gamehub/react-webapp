import React from "react";

const ResetPassSubmitButton = () => {
    return (
        <button className="submit-btn" type="submit">
            Reset Password
        </button>
    );
};

export default ResetPassSubmitButton;
